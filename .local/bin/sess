#!/bin/sh

CTRLPATH=$(grep ControlPath ~/.ssh/config|cut -d' ' -f2|head -n1|sed "s:~:$HOME:g")
CTRLDIR=$(dirname "$CTRLPATH")
list="none"
find_all()
{
	find "$CTRLDIR" -maxdepth 1 -type s -exec basename {} .ssh \;
}
if [ -d "$CTRLDIR" ]; then
	list=$(find_all|sort)	
fi

echo "$list"


